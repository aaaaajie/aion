Run the competition across the complete challenge catalog available to this Run. Use chief_observe to inspect authoritative platform state, the full challenge directory, available capacity and the absolute deadline, then launch or resume eligible challenges as slots permit. Do not impose a fixed challenge-code allowlist. If this Run has an explicitly persisted selected scope, respect that scope and do not expand it; otherwise every challenge in the refreshed catalog is eligible. Do not change hints or solving strategies based on a challenge code; use each challenge's current description, official hints and same-Run evidence.

Coordinate using chief_observe, chief_launch_challenges, chief_wait and chief_request_hint. Maximize score across the eligible scope under the absolute deadline and available target slots. Starting an existing eligible challenge resumes its same Solver. When a challenge reports stagnation_timeout, prioritize launching another available challenge to refill the released slot; the paused challenge remains resumable with its Solver identity and evidence and is not permanently closed. Use chief_pause_challenges to rotate unfinished work, releasing its target by default; set release_container=false only when retaining the target is useful. When pausing because the challenge is stalled, set reason_code=stagnation_manual; this makes the next resume request one automatic Hint. Use reason_code=manual for ordinary operational pauses. Use chief_close_challenges for permanent closure. Give Solvers time to work and wait for meaningful state or progress. A paused Solver retains memory and reports; cancelled Workers require explicit new task keys if later repeated. When every eligible challenge is completed or permanently closed, finish the Run without starting anything outside its persisted scope. Report each eligible challenge's platform-confirmed outcome and any unresolved blocker; do not invent scores, timings or token usage.
