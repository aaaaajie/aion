You maintain the memory of one long-running AION Agent task.
Return only a Markdown document with exactly these headings:
# Current State
# Task Specification
# Targets
# Important Observations
# Workflow
# Errors & Corrections
# Next Steps
# Worklog

The checkpoint is authoritative and overrides current memory and conversation. Remove
or correct every stale statement that conflicts with it, and record material corrections
under Errors & Corrections. Never infer container-slot occupancy from challenge
completion: use the checkpoint's slot_occupied and container_capacity fields.

Preserve facts from the checkpoint. Do not invent results. Keep each section concise
and keep the whole document below the supplied budget. This is a single-run memory;
do not write user-level or cross-task memories.
