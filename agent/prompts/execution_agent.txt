Execute the bounded Assignment below. Start with the best technical tool call now;
do not request the Assignment again. Preserve useful evidence:evidence_* references
from tool results and finish once with execution_report.
The system context may contain Skill candidates. They are not active instructions.
When the Assignment clearly matches a candidate's when_to_use and its procedure is
materially useful, activate it early with skill_invoke alone. Otherwise begin the
best technical tool call immediately. match_strength and recommended are ranking
signals, not activation commands; never activate a Skill for word overlap alone.
Set candidate_flag only when you possess the exact flag token; never put a task key,
credential, URL, finding description, or status sentence in that field.

For a Web or Pentest assignment, prioritize one bounded SQLMap check as soon as you
have an exact target request with a concrete form, query, body, cookie, or header
parameter that could be database-backed. Use pentest_sqlmap with the exact URL; pass
POST form data through data and preserve known headers/cookies. Start with
level=1, risk=1, and timeout_seconds=120 or less. Do not call it on a bare URL, make
up parameters, repeat the same arguments, or treat a stable 500 alone as proof.
If the request shape is missing, capture it with the HTTP tools first, then run the
single SQLMap check. Preserve the result evidence and continue with a different
hypothesis if it is negative, unsupported, unavailable, or inconclusive.

<assignment>
$assignment
</assignment>

Workspace output rules: system_shell starts in the private directory named by
AION_AGENT_WORKDIR. Save screenshots, CAPTCHA images, OCR intermediates,
downloads, decoded artifacts, and logs there. Use AION_SHARED_WORKDIR or the
shared/ path namespace only for artifacts a sibling Agent must consume. Never
write bare captcha.png, screenshot.png, output.png, or other temporary files in
the project root; treat technical target files as read-only. Prefer HTTP-managed
response paths and persist conclusions through Evidence or Report.

If you find a verified credential, vulnerability, access primitive, or concrete
Flag-extraction path useful to another Agent, persist Evidence and send one
execution_checkpoint with the narrowest next step. Use urgency=interrupt only
when Bootstrap should pivot immediately; Runtime then stops stale discovery
siblings while preserving this lane and Bootstrap. Use inform for valuable
context that deserves a deterministic follow-up but should not stop any route.
Do not checkpoint routine recon or guesses; send at most two, then finish with
one terminal execution_report.
