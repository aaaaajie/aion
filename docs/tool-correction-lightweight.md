# 参数恢复与提示的小范围修复

按用户选择只改第 2、3、4 项。未修改 Shell 过滤正则、容器隔离或 HTTP 请求引擎，未部署、未重启线上比赛。

## 改动

- 删除 probe 第二次参数错误后禁用整个会话的逻辑及旧状态字段。不同参数可以继续修正；相同错误参数只追加提醒，保留字段错误与合法纠错能力。
- HTTP 参数错误提供对应的 `system_http_plan` 调用结构。可解析的错误参数保留当前目标；非法 JSON 提供标明为示例的合法结构。模板／文件等带字段位置的语义错误也给出预检建议。
- 执行前拒绝注明 `execution_status=not_started`。Shell 现有容器／安装拦截仍然生效，但回执明确整条命令未执行、不能假定目录或文件已创建。执行中错误不会冒充未执行。
- 通用嵌套网关已删除；纠错只返回通过目标 Schema 校验的 `next_tool` 与 `next_arguments`，不会自动解包执行或保留兼容接口。
- 提示要求首次复杂协议能力精确查 schema、优先发现已有 FastCGI 等工具，复杂矩阵或参数纠错使用预检，普通请求不额外预检。
- 自动复盘提醒明确尽快覆盖所列结果，未知可直接 inconclusive，仍不阻断工具。新增说明：正文完整或 hash 相符不能代替实现校准。

## 轻量验证

定向回归 **31 passed，0.66 秒**。覆盖多次错误→同参数提醒→语义错误→修正后真实 HTTP 执行、原请求网关纠错、执行前／执行中错误区别及既有提示契约。日志：`.aion/verification/tool-usability/correction-targeted.log`。`git diff --check` 通过，未跑完整后端测试。

只执行 **一次**自然任务模型验收，生产提示与实际 Runner/Provider，本地合成 HTTP 服务。用户任务仅描述 2×2 请求比较和顺序回执链，没有点名预检／复盘工具。共 20 次模型请求，69.97 秒；输入 292,521 token（缓存 257,920、未缓存 34,601），输出 8,067，无缺失 usage。没有为取得全绿重复调用模型。

结果：模型先查准确 schema 并主动预检，普通顺序请求未额外预检；主动复盘首次错误地使用校准引用，被正常拒绝后改为 inconclusive，成功覆盖六条结果。复盘及时性未达到本例预期。

**整体验收未通过**：URL 中的 mode 查询参数未到达本地服务；模型读完九份正文，但仍未完整读取引用中的结果列表。两项均记录为后续问题，不扩大本轮修改范围。首次复盘错误后只补了“完整读取不等于校准”的提示句，未再调用模型验证该句。

原始报告：`.aion/verification/tool-usability/live-natural-matrix-and-six-results-45cbd8844d384df99d4a2b9720eb3f96.json`。

验收脚本最初错误地要求覆盖工具结果序号，忽略合法的原生交互完成序号；已按运行时投影修正统计，并用同一次数据库记录只读重算，没有重新跑模型。重算报告为同名前缀的 `-reassessed.json`，仍保留整体失败结论，原始报告不覆盖。
